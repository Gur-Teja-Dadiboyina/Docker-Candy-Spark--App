# Stage 1: Build the static content into the Nginx web server's default directory.
# We use the official Nginx image as the base.
FROM nginx:alpine

# Copy the files into the default Nginx web root directory (/usr/share/nginx/html).
COPY index.html /usr/share/nginx/html/
COPY script.js /usr/share/nginx/html/
COPY style.css /usr/share/nginx/html/

# Optional: If you have the 'candy.png' favicon as referenced in index.html, uncomment and include it:
# COPY candy.png /usr/share/nginx/html/

# Nginx listens on port 80 by default. This EXPOSE instruction informs Docker
# that the container is listening on the specified network ports at runtime.
EXPOSE 80

# The Nginx image is configured to start the server when the container runs.
